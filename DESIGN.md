# DESIGN

(Phase 2+ will expand this.)

Planned components/layouts:
- `BaseLayout` with header/footer
- `ThemeToggle` with accessible dark-mode behavior
- `Seo` helper for metadata + Open Graph tags
- `ContactForm` connected to Cloudflare Worker API
- `PostList` + `Post` rendering (blog)

